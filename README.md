# exp-hapi-redux

Prerequisites:
- Node installation (< v0.12.x)

Install and Run:
- npm install
- npm run build

Production:
- npm run start
or
- npm run prod

Development:
- npm run dev (watch and automatically build server-client js)